import{test,expect} from "@playwright/test";


const searchdata=[{keyword:"playwright,extraTitle"playwright"},{keyword:"Selenium",extraTitle:"Selenium",}],{keyword:}]);
test.describe("Google search using parameters",async({page})=>{
    await page.got("")




    //selectsearch
    await page.locator("textarea")[name='q').fill(data.keyword);
    await page.Keyboard.press("Enter");


    const firstpage.locato9("h3").first();
    const
});
